# Project Brief

## Overview
The goal is to set up a GitHub Workflow to automatically deploy the application when changes are pushed to the `main` branch. The application is a React + TypeScript + Vite project.

## Core Requirements
- Auto-deploy on push to `main`
- Build the Vite application
- Deploy to GitHub Pages (implied by `gh-pages` dependency in package.json)
